{
  "name": "nnrp-js",
  "version": "0.1.0-preview.0",
  "private": true,
  "description": "TypeScript and JavaScript SDK workspace for NNRP.",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "build": "deno task build",
    "format": "deno task format",
    "format:check": "deno task format:check",
    "lint": "deno task lint",
    "test": "deno task test",
    "typecheck": "deno task typecheck"
  },
  "devDependencies": {
    "@types/node": "^20.17.0"
  }
}
